streamlit>=1.32
# starlette é dependência transitiva do Streamlit (não é usada diretamente no
# app), mas precisa ficar travada abaixo de 1.0: a partir da 1.x, o
# GZipResponder passou a exigir um argumento novo (thread_minimum_size) que o
# middleware interno do Streamlit ainda não passa — sem esse teto, um pip
# install novo (ex.: redeploy) resolve a starlette mais recente e o app
# derruba toda conexão com "Connection failed with status 500".
starlette<1.0
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
altair>=5.0
PyGithub>=2.1
